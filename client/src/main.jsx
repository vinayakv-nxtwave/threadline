import React from "react";
import ReactDOM from "react-dom/client";
import ThreadlineCRM from "./threadline-crm.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ThreadlineCRM />
  </React.StrictMode>
);
